import Rebase from 're-base'

const base = Rebase.createClass({
  apiKey: 'KEY HERE',
  authDomain: 'DOMAIN HERE',
  databaseURL: 'DATABASE URL HERE',
  projectId: 'MAYBE A PROJECT ID TOO'
})

export default base
